spring:

  datasource:

    url: ${DB_URL}

    username: ${DB_USERNAME}

    password: ${DB_PASSWORD}

  jpa:

    hibernate:

      ddl-auto: validate

  flyway:

    enabled: true

jwt:

  secret-key: ${JWT_SECRET}

  expiration: 86400000